# v0.7.5 回归测试清单

> 适用版本：v0.7.5（合并上游 0.12.0 系 + 移除测试栈与保存进度定制）
> 测试方式：手动功能回归（本项目已按决策移除自动化测试栈，以构建验证 + 本清单人工回归为准）
> 建议环境：Stash + binge 插件（zip 安装或插件源更新），桌面浏览器 + 移动端各过一遍重点项

优先级说明：P0 = 本次改动直接涉及，必测；P1 = 历史定制受合并影响，应测；P2 = 常规冒烟。

---

## 一、构建与安装（P0）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 1.1 | 插件安装/升级 | Stash → 设置 → 插件，安装或更新到 v0.7.5 | 安装成功，版本号显示 0.7.5 |
| 1.2 | 硬刷新加载 | Ctrl+Shift+R 强刷打开 binge | 界面正常加载，无白屏/控制台报错 |
| 1.3 | 单文件体积 | 检查 dist 产物 | 约 908 kB（gzip 约 245 kB），明显增大属异常需排查 |

## 二、首页 Feed 与 Pack 卡片（P0，合并改动核心区）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 2.1 | Feed 分类切换 | 首页顶部切换 发现/热门/帖子/转发 分类 | 各分类内容正确加载，无报错 |
| 2.2 | Pack 卡片展示 | 找一个批量导入的 Pack 卡 | 4×2 mosaic 正常渲染，"+N"徽章正确 |
| 2.3 | Pack mosaic 点击直达（重点） | 点击 mosaic 任意瓦片 | 直接打开对应场景进入 reel，**不报错**（本轮修复了 tab 未定义导致的点击失效） |
| 2.4 | 无主演 Pack 卡 | 打开无归属的批量导入卡 | 头像区降级显示，mosaic 点击直达同样正常 |
| 2.5 | Pack 详情 sheet | 点击卡片主体打开包列表 | 列表完整滚动，点任一场景可进入播放 |

## 三、图库（P0，本地重构 + 上游修复融合区）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 3.1 | 两层图库导航 | 演员详情 → 图库 tab → 点封面进第二层 → 返回 | 层级切换正常，返回按钮有效 |
| 3.2 | 图库内无限滚动 | 打开图片多的图库，滚到底 | 持续加载不卡死、不重复、不漏图（上游修复 + 本地结构适配） |
| 3.3 | 灯箱翻页 | 第二层点图进灯箱，左右切换/ESC/背景/× 关闭 | 全部关闭路径有效 |
| 3.4 | 封面悬停轮播 | 鼠标悬停图库封面 | 0.5s 后开始每秒换图，移开恢复封面 |
| 3.5 | 首页图库卡片 | 含图库的 feed 卡片进入视口 | 自动轮播 2s/张，离开视口停止 |
| 3.6 | 图库徽章/空状态 | 无图库演员 | 显示"无图库"，不报错 |

## 四、设置页（P0，上游新功能 + 重度冲突区）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 4.1 | 设置搜索 | 设置页输入关键词（如"流"） | 实时过滤出匹配项，清空按钮可用 |
| 4.2 | 搜索无结果 | 输入乱码关键词 | 显示空结果提示，无报错 |
| 4.3 | 分区结构 | 浏览整个设置页 | 动态/外部来源等分区标题与说明均为中文 |
| 4.4 | 伴侣插件状态 | 查看插件区块 | 已安装/未安装/获取 三态显示正确（依赖 Stash 插件列表） |
| 4.5 | 语言切换 | 中文 ↔ English 切换 | 全部文案即时切换，刷新后偏好保留 |
| 4.6 | 常规设置项 | 流类型/近期窗口/监看质量等逐项改一次 | 保存生效，无 JS 报错 |

## 五、保存到 Stash / PH 播放器（P0，本轮删除定制区）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 5.1 | PH 保存按钮四态 | 在 PH 播放器中点"保存到 Stash" | idle → 保存中 → 已保存/重试，四态切换正常 |
| 5.2 | 保存中无进度条（重点） | 保存大视频期间观察按钮 | **不显示百分比和进度填充条**（定制已移除），仅显示"保存中"文字 |
| 5.3 | 保存结果 | 保存完成后到 Stash 查验 | 场景入库，元数据（工作室/标签/演员/日期）后台补全 |
| 5.4 | 保存失败路径 | 断开 daemon 或用无效配置保存 | 按钮显示"重试"，不永久卡在"保存中" |
| 5.5 | 保存超时 | 正常保存流程等待 | 30s 超时上限生效（不再有 4 分钟长等待） |
| 5.6 | PH 缩略图代理 | PH 相关卡片/瓦片图片加载 | 缩略图正常显示（上游基础代理功能保留） |
| 5.7 | Reddit/X 保存 | 各来源的内容保存 | 其余来源保存流程不受影响 |

## 六、StashDB 发现与关注（P1）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 6.1 | 发现/热门卡片 | 首页浏览 StashDB 卡片 | 正常加载，图片/链接/保存入口可用 |
| 6.2 | 网络降级 | 短暂断网后恢复，再触发 StashDB 请求 | 恢复后数据正常返回，**不出现长期空数据**（瞬时故障不覆盖缓存） |
| 6.3 | 关注弹窗 | 关注一个 StashDB 演员 | "正在查找场景"及失败提示显示为翻译文案（非 key 原文） |
| 6.4 | 演员页混入场景 | 打开关注演员详情 | StashDB 场景正确混入档案网格 |

## 七、评分与合集（P1）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 7.1 | 多维评分保存 | CriterionRatingModal 保存评分 | 成功/失败提示均为翻译文案；"已保存未回读"警告显示正常 |
| 7.2 | 默认合集保护 | 尝试删除收藏夹/稍后观看/我的最爱 | 删除被拒绝，确认弹窗显示保护提示 |
| 7.3 | 合集保存流程 | 保存场景到各合集 | SaveSheet 图标正确、场景入合集 |
| 7.4 | 老用户合集迁移 | 用旧版升级上来的库 | 默认合集正常存在，无重复建 tag |

## 八、Stories 与播放器（P1，历史定制回归）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 8.1 | Stories 自动播放 | 点有故事的演员头像 | 视频自动播放（本轮修复回归点） |
| 8.2 | 弹窗交互 | 卡片内点按暂停/翻页、点空白关闭、ESC | 全部路径有效（v0.7.4 修复保持） |
| 8.3 | 窄屏 Stories | ≤720px / <560px 视口 | 卡片居中、等比收缩 |
| 8.4 | 进度条拖动擦洗 | 播放页拖动进度条 | 时间码气泡实时、松手跳转，转码流不崩（v0.7.0–0.7.2 保持） |
| 8.5 | 观看完整场景 | feed 卡点"观看完整场景 →" | chained 模式以该场景为种子进 reel（硬约束） |
| 8.6 | 转码容器播放 | 播放 avi/wmv/mkv 等 | 自动走 MP4 转码流，快进正常 |
| 8.7 | URL 安全过滤 | 含外部链接的帖子（重定向/异常链接） | 异常链接被拦截处理，正常链接可打开 |

## 九、i18n 完整性（P1）

| # | 测试项 | 步骤 | 预期 |
|---|--------|------|------|
| 9.1 | key 原文泄漏检查 | 全界面走查中英文各一遍 | 不出现 `follow.xxx` 这类未翻译 key 原文（本轮补了 8 个 key） |
| 9.2 | 新功能文案 | 设置搜索/伴侣插件/关注弹窗/评分警告 | 全部为中文文案 |
| 9.3 | 插值变量 | 带 {{count}} 等插值的文案 | 数值正确替换 |

## 十、常规冒烟（P2）

| # | 测试项 | 预期 |
|---|--------|------|
| 10.1 | 底部导航各页切换 | 无白屏/报错 |
| 10.2 | Reel 滑动/双击点赞/操作栈 | 常规交互正常 |
| 10.3 | 演员档案信息展示 | 简介统计、社交图标、日期格式 `YYYY年M月D日` |
| 10.4 | 演员详情 tab 切换 | 不抖动不闪跳（v0.7.3 保持） |
| 10.5 | 移动端整体走查 | 触屏操作、底部导航、卡片布局正常 |

---

## 记录模板

发现问题按此格式记录，便于回溯：

```
### [清单编号] 问题标题
- 环境：桌面/移动端、浏览器、Stash 版本
- 现象：
- 复现步骤：
- 期望：
- 截图/控制台日志：
```
