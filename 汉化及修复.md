# Binge 插件汉化及修复文档

> **插件版本**: v0.4.0  
> **原始来源**: https://github.com/ordureconnoisseur/binge/releases/tag/v0.4.0  
> **文档用途**: 插件升级后快速还原所有汉化和功能修改

---

## 一、硬约束

- 用户可见的 UI 文本必须翻译为中文
- 品牌名和技术术语保持英文：Stash、Reddit、X (Twitter)、Cookie、forage、binge-server、HLS、MP4、WebM、StashDB、PornHub
- LLM 提示词（prompt）不翻译，以保持模型输出行为
- 首页"观看完整场景 →"按钮使用 `chained` 模式而非 `pinnedQueue`
- 演员档案页"图库"tab（原"照片"tab）显示 Stash 图库而非单张图片
- 日期格式使用中文 `YYYY年M月D日`（通过 Zo 函数）

## 二、工程规范

- 插件目录只保留 4 个核心文件：`binge.entry.js`、`binge.yml`、`index.html`、`manifest`
- ReelView useEffect 依赖数组必须包含 `pinnedQueue` 和 `pinFirstSceneId`
- 图库封面使用 3:4 竖屏比例（`aspect-ratio:3/4` + `object-fit:cover`）
- 图库图片单次最多加载 500 张
- 修改大文件前必须备份，编码统一使用 UTF-8
- 详情页图库封面悬停：setTimeout 0.5 秒获取图片，加载后立即切换下一张，setInterval 每秒循环
- 首页图库卡片自动播放：IntersectionObserver 50% 可见时启动，setInterval 2 秒切换
- 视频 poster 懒加载：Gc 组件 useEffect 添加 `!h` 守卫，仅视口内卡片加载视频源

## 三、经验教训

- 中文字符编码损坏：EF BF BD 3F（U+FFFD + '?'）会替换某些多字节 UTF-8 字符（如→、—、在、到、的、随等），导致 UI 乱码；修复需字节级替换，不能依赖字符串级操作
- `koa-connect` 包装器导致 ctx 泄漏，需原生 Koa 重写
- 多视图队列每 5 秒轮询 `{ configuration { plugins } }`（41 字节），用于跨标签页同步 multiView.queue，仅在页面可见且无写入冲突时发送，属正常心跳请求
- `pinnedQueue` 导航模式导致 scrollTo() 值被虚拟列表逻辑覆盖
- 压缩 JS 全局替换（如 `;;` → `;`）会破坏 `for(;;)` 循环和 `for...in/of` 语法
- 模板字面量中的三目表达式复数形式必须简化为中文（中文无复数）
- 函数名不能与 React 内部变量冲突（如 `U`、`W` 已冲突需重命名为 `mU`、`mW`；`Uf`、`Lu`、`za` 组件/函数也需重命名为 `mUf`、`mLu`、`mza`）
- Stash Gallery 类型不存在 `image_path` 字段，查询会返回 400 错误
- `w.length>0` 在 `w` 初始值为 null 时报错，需用 `w?.length>0` 可选链
- `new Set()` 必须加括号，否则逗号运算符导致返回数组而非 Set，后续 `.has()` 报 TypeError

---

## 四、本次会话代码修改详情

### 修改 1：新增 PerformerGalleries GraphQL 查询

**位置**: index.html 第 800-823 行（在 `ta=` 查询之前插入）

```javascript
bs=`
    query PerformerGalleries($id: ID!, $page: Int!, $per_page: Int!) {
        findGalleries(
            gallery_filter: {
                performers: { value: [$id], modifier: INCLUDES }
            }
            filter: {
                page: $page
                per_page: $per_page
                sort: "created_at"
                direction: DESC
            }
        ) {
            count
            galleries {
                id
                title
                image_count
                cover { paths { thumbnail } }
            }
        }
    }
`,
```

**说明**: 按演员 ID 分页查询关联图库，返回 count、id、title、image_count 和封面缩略图。

### 修改 2：新增 pk() 数据获取函数

**位置**: index.html 第 396 行（在 `V` 函数之后、`Ni` 函数之前插入）

```javascript
async function pk(e,t,n){
    let r=await z(bs,{id:e,page:t,per_page:n});
    return{
        count:r.findGalleries.count,
        galleries:r.findGalleries.galleries.map(e=>({
            id:e.id,
            title:e.title,
            image_count:e.image_count,
            cover:e.cover
        }))
    }
}
```

**说明**: 调用 `bs` 查询，返回 `{count, galleries}` 结构。被 yf 组件调用以加载演员图库数据。

### 修改 3：yf 组件重构（照片网格 → 图库网格 + 灯箱）

**位置**: index.html 第 1044 行

**修改内容**:
1. **第一层**: 图库封面网格（`binge-gallery-grid` 类），3 列布局，封面使用 3:4 竖屏比例
2. **第二层**: 点击封面进入图库内图片网格（`binge-profile-photo-grid` 类），复用已有的 `ta`（GalleryImages）查询加载图片
3. **灯箱**: 点击图片打开 `mf` lightbox 组件，支持左右箭头切换、ESC/背景/× 关闭
4. **返回按钮**: `← 返回` 按钮从第二层返回第一层
5. **空状态**: 无图库时显示 `无图库` 文本

**新增 CSS 类**:
- `.binge-gallery-grid` — 图库封面网格容器
- `.binge-gallery-cover` — 封面图片，`aspect-ratio:3/4; object-fit:cover; background-color:#ffffff0d`
- `.binge-gallery-cell` — 单个图库单元格
- `.binge-gallery-cell-title` — 图库标题，单行省略号
- `.binge-gallery-back` — 返回按钮
- `.binge-gallery-title` — 第二层图库标题

**Tab 变更**:
- Tab ID: `photos` → `galleries`
- Tab 标签: `照片` → `图库`

### 修改 4：时间函数翻译

**位置**: index.html 第 1044 行（um 函数和 Bf 函数）

| 原文 | 译文 | 说明 |
|------|------|------|
| `just now` | `刚刚` | um 函数，<60秒 |
| `${i} min ago` | `${i} 分钟前` | um 函数，<60分钟 |
| `${a} h ago` | `${a} 小时前` | um 函数，<24小时 |
| `${Math.floor(a/24)} d ago` | `${Math.floor(a/24)} 天前` | um 函数，>=24小时 |
| `${o}w` | `${o}周` | Bf 函数，<5周 |
| `${s}mo` | `${s}个月` | Bf 函数，<12个月 |
| `${Math.floor(a/365)}y` | `${Math.floor(a/365)}年` | Bf 函数，>=12个月 |

### 修改 5：修复 React 内部函数名冲突（Uf / Lu / za）

**位置**: index.html

**问题**: 应用级组件/函数 `Uf`（头像栈）、`Lu`（useStoryViewer）和 `za`（转发图标）与 React 内部同名函数冲突。后定义的应用级函数覆盖了 React 内部函数，导致推荐页面渲染崩溃，头像框右下角转发小图标不显示。

**修复**: 将应用级函数重命名并同步更新调用点：

| 原函数名 | 新函数名 | 用途 | 调用点数量 |
|---------|---------|------|-----------|
| `Uf` | `mUf` | 头像栈组件 | 1 定义 + 3 调用 |
| `Lu` | `mLu` | useStoryViewer hook | 1 定义 + 6 调用 |
| `za` | `mza` | 转发 SVG 图标组件 | 1 定义 + 2 调用 |

**验证**: React 内部函数 `Uf(e,t,n)`、`Lu(e,t,n,r,a,o,s,c,l)` 和 `za(e,t)` 未被改动，保留其原始行为。

### 修改 6：修复首页白色文字白色背景异常

**位置**: index.html CSS（`.binge-app.refract .binge-feed-card` 和 `.binge-app.refract .binge-discovery-card`）

**问题**: refract 主题下卡片使用 `background:var(--glass-bg,#ffffff0a)`，当 CSS 变量未正确继承或 `backdrop-filter` 不生效时，回退值 `#ffffff0a` 接近白色，与白色文字冲突。

**修复**: 
- 将 `--glass-bg` 回退值改为 `#00000080`
- 为 Feed 卡片和 Discovery 卡片添加 `background-color:#0d0d0d` 深色底衬
- 添加无前缀 `backdrop-filter:blur(20px)` 兼容 Firefox
- 将 Stack avatar 边框回退值从 `#ffffff0a` 改为 `#00000080`

### 修改 7：Feed 卡片头像栈集成

**位置**: index.html 第 1044 行（`Vf` 组件和 `Uf`/`mUf` 组件）

**修改内容**:
1. 在 `Vf` 组件中获取故事数据：`{open:b}=mLu(),x=Ef(),S=(0,y.useMemo)(()=>x.state.kind===`ready`?new Set(x.state.stories.map(e=>e.performerId)):new Set(),[x.state])`
2. 将 `storyPerformerIds:S` 传递给 `mUf` 组件
3. `mUf` 组件根据 `storyPerformerIds.has(e.id)` 判断是否为有故事的演员，添加 `binge-feed-card-stack-story-ring` 彩色边框
4. 转发内容时显示右下角转发小图标（`binge-feed-card-stack-repost-badge`）

### 修改 8：图库封面悬停自动播放（mYf 组件）

**位置**: index.html 第 1044 行（`mYf` 组件，嵌套在 `yf` 组件的 `binge-gallery-grid` 中）

**修改内容**:
1. 新增 `mYf` 组件，替代原来 `yf` 组件中 `binge-gallery-grid` 的内联 `li` 渲染
2. 使用 `useRef` 管理：`n`（img 元素引用）、`a`（当前图片索引）、`o`（图片数组缓存）、`r`（setTimeout ID）、`i`（setInterval ID）、`h`（hover 状态守卫）
3. **悬停逻辑**：
   - `onMouseEnter`：设置 `h.current=!0`，启动 `setTimeout(l,5e2)`（0.5 秒延迟获取图片）
   - `l` 异步函数：调用 `Ii(e.id,50)` 获取图库图片（最多 50 张），加载后立即切换到下一张，然后启动 `setInterval` 每秒循环切换
   - 鼠标离开期间若图片尚未加载完，`h.current` 守卫阻止启动 `setInterval`
   - `onMouseLeave`：清除所有定时器，重置 hover 守卫，恢复封面图
4. **`useEffect` 清理**：返回时 `clearTimeout(r.current)` 和 `clearInterval(i.current)`

**关键代码片段**:
```javascript
let l=async()=>{
  if(!o.current){
    try{o.current=await Ii(e.id,50)}catch(u){return}
  }
  if(!h.current)return;
  if(o.current&&o.current.length>1&&n.current){
    a.current=(a.current+1)%o.current.length,
    n.current&&(n.current.src=o.current[a.current]?.paths?.thumbnail||``),
    i.current=setInterval(()=>{
      a.current=(a.current+1)%o.current.length,
      n.current&&(n.current.src=o.current[a.current]?.paths?.thumbnail||``)
    },1e3)
  }
};
// onMouseEnter: h.current=!0, r.current=setTimeout(l,5e2)
// onMouseLeave: 清除定时器 + 恢复封面
```

### 修改 9：视频 poster 懒加载（Gc 组件）

**位置**: index.html 第 1044 行（`Gc` 组件 useEffect）

**修改内容**:
1. 在视频源加载的 useEffect 中添加 `!h` 条件，仅当卡片进入视口（`h` 为 true）时才设置视频 `src` 并加载
2. 卡片不在视口时：视频源不加载，只显示 `poster:e.paths.screenshot` 封面图作为占位
3. 卡片进入视口时：加载视频源并自动播放
4. 卡片离开视口时：IntersectionObserver 触发 `n.pause()` 暂停视频

**修改前**:
```javascript
(0,y.useEffect)(()=>{let t=p.current;if(!t||l||qe())return;let n=Qa(e,f);t.src!==n&&(t.src=n,t.load()),h&&t.paused&&z(t)},[l,e.id,h,f])
```

**修改后**:
```javascript
(0,y.useEffect)(()=>{let t=p.current;if(!t||l||qe()||!h)return;let n=Qa(e,f);t.src!==n&&(t.src=n,t.load()),t.paused&&z(t)},[l,e.id,h,f])
```

**说明**: 原逻辑是无论卡片是否在视口内都加载视频源，仅在播放时检查 `h`。修改后，视频源（可能数十 MB）只在卡片可见时才请求，大幅减少不可见卡片的带宽和内存占用。

### 修改 10：首页图库卡片自动播放（Kf 组件）

**位置**: index.html 第 1044 行（`Kf` 组件 useEffect）

**修改内容**:
1. 首页图库卡片（`binge-feed-card-gallery`）使用 `IntersectionObserver` 监听卡片是否进入视口（`threshold:.5`，即 50% 可见）
2. 进入视口且图片数 >1 时，启动 `setInterval` 自动轮播，每 **2 秒** 切换到下一张（`scrollTo({left:(n+1)%c*carousel.clientWidth,behavior:`smooth`})`）
3. 离开视口时 `clearInterval` 停止轮播
4. 组件卸载时 `disconnect` observer 并清理 interval

---

## 五、完整翻译清单

> 以下为 index.html 中所有中文翻译，按类别分组。升级后可用搜索替换方式还原。

### 5.1 导航/菜单

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 首页 | Home | Reel 分区标签 + 键盘 Home 键 |
| 关注中 | Following | Reel 分区标签 |
| 推荐 | For you | Reel 分区标签 |
| 发现 | Explore / Discover | Reel 分区标签 |
| 菜单 | Menu | 菜单按钮 title |
| 设置 | Settings | 菜单项标题 |
| 图库 | Gallery | Tab 标签 + 标题 |
| 收藏夹 | Favourites | 演员分组标题 |
| 所有演员 | All performers | 演员分组标题 |
| 稍后观看 | Watch later | 合集名称 |
| 活跃 | Active | 标题 |
| 热门 | Trending | 来源标签 |
| 已保存的筛选 | Saved filters | 筛选面板标题 |
| 故事查看器 | Story viewer | 对话框 aria-label |
| 图片查看器 | Image viewer | 对话框 aria-label |
| 场景详情 | Scene details | 对话框 aria-label |
| 标签页 | Tabs | tablist aria-label |
| 分区 | Sections | Reel tablist aria-label |
| 图库位置 | Gallery position | tablist aria-label |
| 档案内容 | Profile content | tablist aria-label |
| 返回 | Back | 返回按钮 |
| 返回收藏 | Back to favourites | aria-label |
| 返回首页 | Back to home | aria-label |
| 搜索场景 | Search scenes | placeholder + aria-label |
| 搜索演员 | Search performers | placeholder + aria-label |
| 发现演员 | Discover performers | 标题 |
| 打开档案 | Open profile | 按钮 |
| 在新标签页中打开 | Open in new tab | 选项 sub 文本 |
| 新场景和图库 | New scenes & galleries | feed aria-label |
| 有新场景的演员 | Performers with new scenes | stories row aria-label |
| 上一张 | Previous | 图片轮播 aria-label |
| 下一张 | Next | 图片轮播 aria-label |
| 上一页 | Previous page | 分页按钮 aria-label |
| 下一页 | Next page | 分页按钮 aria-label |
| 向右滚动 | Scroll right | 滚动按钮 aria-label |
| 向左滚动 | Scroll left | 滚动按钮 aria-label |
| 标签向右滚动 | Scroll tags right | 滚动按钮 aria-label |
| 标签向左滚动 | Scroll tags left | 滚动按钮 aria-label |
| 跳转到第 | Jump to | 圆点 aria-label 前缀 |

### 5.2 按钮/动作

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 关闭 | Close | 关闭按钮 aria-label |
| 关闭档案 | Close profile | aria-label |
| 取消 | Cancel | 按钮 |
| 创建 | Create | 按钮 |
| 删除 | Delete | 按钮 |
| 刷新 | Refresh | 菜单行 |
| 刷新故事 | Refresh story | aria-label + title |
| 保存到 | Save to | 标题 + 按钮 title |
| 保存场景 | Save scene | aria-label |
| 保存评价和评分 | Save review and rating | 按钮 |
| 仅保存评价 | Save review only | 按钮 |
| 保存失败 | Save failed | title 提示 |
| 加入库 | Add to library | 按钮 + 标题 |
| 将场景加入库 | Add scene to library | aria-label + 标题 |
| 将演员加入库 | Add performer to library | aria-label |
| 加入多视图 | Add to multiview | aria-label |
| 发送到多视图 | Send to multiview | title |
| 移出多视图 | Remove from multiview | aria-label |
| 添加到 | Add to | 按钮 |
| 添加中 | Adding… | 按钮状态 |
| 重试 | Retry | 按钮 |
| 重新加载 | Reload | 按钮 |
| 重新开始 | Restart | 按钮 |
| 继续 | Continue | aria-label |
| 暂停 | Pause | aria-label |
| 播放 | Play | aria-label |
| 收藏 | Favourite | 按钮 |
| 已收藏 | Favourited | 按钮状态 |
| 关注 | Follow | 按钮 |
| 已关注 | Following | title |
| 取消静音 | Unmute | title + aria-label |
| 静音 | Mute | title + aria-label |
| 喜欢 | Like | aria-label + title |
| 点击喜欢 | Click to like | title |
| 长按取消 | Long-press to unlike | title |
| 长按删除 | Long-press to delete | title |
| 点击打开 | Click to open | title |
| 长按打开多视图 | Long-press to open multiview | title |
| 点击排队 | Click to queue | title |
| 打开 | Open | aria-label + title |
| 打开包 | Open pack | aria-label |
| 打开场景 | Open scene | aria-label |
| 打开筛选 | Open filters | aria-label |
| 打开菜单 | Open menu | aria-label |
| 排序场景 | Sort scenes | title |
| 排序演员 | Sort performers | aria-label |
| 更多 | More | title + 按钮 |
| 更多操作 | More actions | aria-label + title |
| 收起 | Collapse / Show less | 按钮 |
| 显示另外 | Show N more | aria-label 前缀 |
| 显示更少标签 | Show fewer tags | aria-label |
| 显示其他链接 | Show other links | aria-label |
| 显示详情 | Show details | aria-label |
| 移除 | Remove | title + aria-label |
| 从筛选中移除 | Remove from filters | aria-label |
| 清除 | Clear | aria-label + title |
| 清除全部 | Clear all | 按钮 |
| 清除此评分 | Clear this rating | aria-label |
| 筛选 | Filter | aria-label + title |
| 筛选动态 | Filter feed | aria-label + title |
| 管理保存位置 | Manage save locations | aria-label |
| 管理关注 | Manage follows | title |
| 写评价 | Write review | aria-label + title |
| 手动撰写 | Write manually | 按钮 |
| 开始 | Start | 按钮 |
| 返回访谈 | Back to interview | 按钮 |
| 根据已有对话生成评价 | Generate review from conversation | title |
| 放弃此次会话并开始新访谈 | Abandon session and start new interview | title |
| 查看 | View | 链接文本 |
| 查看图库 | View gallery | 文本 |
| 查看完整图库 | View full gallery | aria-label |
| 查看故事 | View story | title |
| 观看完整场景 | Watch full scene | 按钮 |
| 场景操作 | Scene actions | aria-label |
| 在动态中显示 | Show in feed | 标题 |
| 新建合集 | New collection | 按钮 |
| 在 Stash 中创建场景并链接到 StashDB | Create the scene in Stash + link to StashDB | 菜单项 sub |
| ← 返回 | ← Back | 图库返回按钮（本次新增） |

### 5.3 状态/提示

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 保存中 | Saving… | 保存状态 |
| 已保存 | Saved | 状态/菜单 |
| 已保存。 | Saved. | 状态消息 |
| 已设置 | Set | placeholder 前缀 |
| 已在 | Already in | 状态文本 |
| 加载中 | Loading | 状态 |
| 加载更多 | Load more | 加载状态 |
| 加载场景中 | Loading scene… | 状态 |
| 获取元数据 | Fetching metadata | 状态 |
| 获取场景元数据 | Fetching scene metadata | 状态 |
| 未知 | Unknown | 文本 |
| 不在库中 | not in library | 文本 |
| 不在你的库中 | not in your library | title |
| 已加入你的库 | already in your library | title |
| 已评 | Rated | aria-label 前缀 |
| 星。点击修改。 | stars. Click to modify. | aria-label 片段 |
| 不写此评分 | Don't write this rating | title |
| 已到底 | End reached | 空状态文本 |
| 项 | items | 量词 |
| 张 | (量词) | aria-label 量词 |
| 个新 | new | title/aria-label |
| 个其他链接 | other links | title |
| 个标签 | tags | aria-label |
| 名演员 | performers | 文本 |
| 包含 | Contains | 文本 |
| 来自 | From | title 前缀 |
| 自动滚动 | Auto-scroll | 文本 |
| 当前场景播放结束时自动进入下一场景 | Auto-advance when current scene ends | sub |
| 下一条消息生效 | Applies to next message | title |
| 合集名称 | Collection name | placeholder |
| 评价文本 | Review text | placeholder |
| 输入你的回答 | Enter your answer | placeholder |
| 在库中 | in library | 文本 |
| 帖子 | Posts | 文本/设置 |
| 新增 | New | 文本 |
| 新发布 | New releases | 设置标题片段 |
| 转发 | Repost / Reposts | title + 文本 |
| 转发了 | Reposted | 文本 |
| 排序 | Sort | 文本 |
| 平均评分 | Average rating | 文本 |
| 总计 | Total | 文本 |
| 计数 | count | aria-label |
| 高潮 | O | 标签 |
| 个场景 | scenes | 文本 |
| 个新场景 | new scenes | 文本 |
| 张照片 | photos | 文本 |
| 无图库 | no photos | 空状态（本次新增） |

### 5.4 设置/说明

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 流媒体类型 | Streaming type | 设置标题 |
| 近期窗口 | Recent window | 设置标题 |
| 在动态中显示图库 | Show galleries in feed | 设置标题 |
| 在故事中包含 | Include in stories | 设置标题前缀 |
| 在档案中包含 | Include in profile | 设置标题前缀 |
| 如何找到你的 | How to find your | 帮助文本前缀 |
| 在普通浏览器标签页中登录 | Log in to ... in a normal browser tab | 帮助 li |
| 打开开发者工具 | Open DevTools | 帮助 li |
| 找到名为 | Find the one named | 帮助 li |
| 每几个月过期。当故事停止更新时，重复步骤 | Cookies expire every few months... | 帮助 li |
| 建议使用副 | Recommend using a secondary | 帮助 li |
| 守护进程代表你轮询 | Daemon polls on your behalf | 设置说明 |
| 监看质量 | Watch quality | 设置标题 |
| 发送场景到 | Send scene to | 设置说明前缀 |
| 元数据提供商 | Metadata providers | 设置路径片段 |
| 场景混入演员档案 | Mix scenes into performer profile | 设置标题 |
| 场景混入此演员的网格 | Mix scenes into this performer's grid | title |
| 展示模式（模糊所有媒体） | Presentation mode (blur all media) | 设置标题 |
| 显示调试覆盖层 | Show debug overlay | 设置标题 |
| 调试覆盖层 | Debug overlay | label |
| 跟随 | Follow | 设置标题 + label |
| 跟随 refract 强调色 | Follow refract accent | 设置标题 |
| 强调色 | accent | 设置标题片段 |
| 显示性别 | Show genders | 设置标题 + aria-label |
| 忽略自动标签 | Ignore auto-tags | label |
| 插件钩子将锁定此设置。 | Plugin hook will lock this setting. | small 文本 |
| 评分（高级评分） | Ratings (advanced ratings) | 标题 |
| forage 服务器 URL | forage server URL | 设置标题 |
| binge-server 配置 | binge-server config | 标题 |
| 自动（Stash 决定） | Auto (Stash decides) | option |
| 直连（无转码） | Direct (no transcode) | option |
| 转码 MP4 | Transcode MP4 | option |
| 转码 WebM | Transcode WebM | option |
| 演示内容 | Demo content | 设置标题 + label |
| 展示模糊 | Show blur | label |
| 已收藏场景的自定义合集。 | Custom collections of favourited scenes. | 描述 |
| 帖子写入位置。两个路径是因为守护进程和... | posts write location... | 设置说明 |
| 内容的回溯范围。同时影响故事栏和初始动态加载... | Lookback range of content... | 设置说明 |
| 它们会自动出现在这里。 | they'll appear here automatically. | 空状态 |
| 女性跨性别女性；切换其他以扩大范围。 | transgender female; toggle others to widen. | 设置说明 |
| 用于截图、演示录制或屏幕共享而不暴露库内容。 | For screenshots/demos/screen sharing... | 设置说明 |
| 不会上传或修改任何内容；仅为浏览器内的显示滤镜。 | Won't upload or modify... | 设置说明 |
| 故事栏也会展示你库中演员在 StashDB 上的新发布 | Story bar also shows... new releases on StashDB | 设置说明 |
| 查看库中演员档案时，也展示其 StashDB 目录中你尚未拥有的场景 | When viewing library performer profile... | 设置说明 |
| 视频如何传输到 binge reel。自动跟随 Stash 的转码规则。 | How video is transmitted to binge reel... | 设置说明 |
| 故事栏展示个人资料包含 pornhub.com 链接的演员 | Story bar shows performers with pornhub.com links | 设置说明 |
| 帖子。需要 binge-server 运行 | posts. Requires binge-server running | 设置说明 |
| 标签页，按需获取。需要 binge-server 运行 | tab, fetched on demand. Requires binge-server | 设置说明 |
| 守护进程写入路径 | Daemon write path | placeholder |
| 如果你也使用 refract 主题 | If you also use refract theme | 设置说明 |
| 请先在设置中添加 forage 服务器 URL。 | Please add forage server URL in settings first. | 错误消息 |
| forage URL 为 http:// 但 binge 通过 https 加载 | forage URL is http:// but binge loaded via https | 错误消息 |
| 不会将 Stash API 密钥发送到不受信任的 URL | Won't send Stash API key to untrusted URL | 错误消息 |

### 5.5 演员信息

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 名称 | Name | 表单字段 + 排序 |
| 消歧义 | Disambiguation | 表单字段 |
| 别名 | Aliases | 标签 |
| 别名（逗号分隔） | Aliases (comma-separated) | 表单字段 |
| 性别 | Gender | 表单字段 |
| 女性 | Female | 性别选项 (FEMALE) |
| 男性 | Male | 性别选项 (MALE) |
| 跨性别女性 | Transgender female | 性别选项 |
| 跨性别男性 | Transgender male | 性别选项 |
| 间性 | Intersex | 性别选项 |
| 非二元 | Non-binary | 性别选项 |
| 出生日期 | Birthdate | 表单字段 |
| 去世日期 | Death date | 表单字段 |
| 国家 | Country | 表单字段 |
| 种族 | Ethnicity | 表单字段 |
| 发色 | Hair color | 表单字段 |
| 眼睛颜色 | Eye color | 表单字段 |
| 身高 | Height | 表单字段 |
| 体重 | Weight | 表单字段 |
| 三围 | Measurements | 表单字段 |
| 乳房假体 | Breast implants | 表单字段 |
| 割包皮 | Circumcised | 表单字段 |
| 已割 | Cut | 选项 |
| 未割 | Uncut | 选项 |
| 阴茎长度 | Penis length | 表单字段 |
| 纹身 | Tattoos | 表单字段 |
| 穿环 | Piercings | 表单字段 |
| 职业开始 | Career start | 表单字段 |
| 职业结束 | Career end | 表单字段 |
| 链接（每行一个） | Links (one per line) | 表单字段 |
| 图片链接 | Image URL | 表单标签 |
| 演员档案 | Performer profile | aria-label |
| 此场景的演员 | Performers in this scene | aria-label |
| 演员 | Performer(s) | 标题 |
| 演员： | Performers: | 标签 |
| 工作室： | Studio: | 标签 |
| 其场景贡献之和，单场景可多次贡献 | sum of scene contributions... | 文本 |
| 库统计： | Library stats: | 文本 |

### 5.6 场景/技术信息

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 场景 | Scene(s) | 通用文本 |
| 技术信息 | Technical info | 标题 |
| 路径 | Path | 标签 |
| 分辨率 | Resolution | 标签 |
| 大小 | Size | 标签 |
| 时长 | Duration | 标签 |
| 帧率 | Frame rate | 标签 |
| 码率 | Bit rate | 标签 |
| 视频 | Video | 标签 |
| 音频 | Audio | 标签 |
| 文字帖 | Text post | aria-label |
| 照片 | Photo | aria-label |
| 图片 | Image | title/aria-label |
| 图库图片 | Gallery images | aria-label |
| 库中的场景 | scenes in library | 文本 |
| StashDB 上的场景 | scenes on StashDB | 文本 |

### 5.7 时间/日期

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 刚刚 | just now | 相对时间 <60秒 |
| 分钟前 | min ago | 相对时间 <60分钟 |
| 小时前 | h ago | 相对时间 <24小时 |
| 天前 | d ago | 相对时间 >=24小时 |
| 周 | w | 相对时间 <5周 |
| 个月 | mo | 相对时间 <12个月 |
| 年 | y | 相对时间 >=12个月 |
| 年 | year | 日期单位 |
| 月 | month | 日期单位 |
| 日 | day | 日期单位 |
| 天 | day(s) | 时间单位 |
| 小时 | hour(s) | 时间单位 |
| 近 | recent / last | 时间前缀（"近 30 天"等） |
| 最近 | Recent | 排序选项 |

### 5.8 排序选项

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 最近 | Recent | 排序 key=recent, stashSort=date |
| 最多播放 | Most played | 排序 key=views, stashSort=play_count |
| 最多高潮 | Most orgasms | 排序 key=orgasms, stashSort=o_counter |
| 最高评分 | Highest rated | 排序 key=rating, stashSort=rating |
| 最近添加 | Recently added | 排序 key=added, stashSort=created_at |
| 最多场景 | Most scenes | 排序 option |
| 最少场景 | Fewest scenes | 排序 option |
| 最新帖子 | Newest posts | 排序 option |
| 最旧帖子 | Oldest posts | 排序 option |

### 5.9 动态类型

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 发现 | Discover | fp.discover |
| 热门 | Trending | fp.trending |
| 帖子 | Posts | fp.posts |
| 转发 | Reposts | fp.reposts |

### 5.10 评分维度

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 总体 | Overall | 评分维度 |
| 默契度 | Chemistry | 评分维度 |
| 美感 | Aesthetics | 评分维度 |
| 制作质量 | Production quality | 评分维度 |
| 创意 | Creativity | 评分维度 |
| 外形 | Physical | 评分维度组 |
| 表现 | Performance | 评分维度组 |
| 面部 | Face | 评分维度 |
| 身体 | Body | 评分维度 |
| 胸部 | Breasts | 评分维度 |
| 臀部 | Ass | 评分维度 |
| 生殖器 | Genitals | 评分维度 |
| 技巧 | Technique | 评分维度 |
| 能量 | Energy | 评分维度 |
| 骚度 | Sluttiness | 评分维度 |

### 5.11 错误/空状态

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 无场景 | No scenes | 状态文本 |
| 无场景匹配此筛选。 | No scenes match this filter. | 空状态 |
| 库中无场景。 | No scenes in library. | 空状态 |
| 无匹配场景。 | No matching scenes. | 空状态 |
| 无图片 | No image | 文本 |
| 无描述。 | No description. | 空状态 |
| 无筛选条件 | No filters | 空状态 |
| 显示全部内容。 | Showing all content. | 空状态 |
| 无已保存的场景筛选。 | No saved scene filters. | 空状态 |
| 无法加载： | Failed to load: | 空状态 |
| 所有内容被筛选掉了 | Everything filtered out | 空状态 |
| 调整筛选条件。 | Adjust filters. | 空状态 |
| 此合集尚未保存场景。 | This collection has no saved scenes. | emptyMessage |
| 空 | Empty | 文本 |
| 视频播放失败 | Video playback failed | 错误文本 |
| 遇到错误 | encountered an error | 错误文本 |
| 错误 | Error | 文本 |
| 点击重试 | Click to retry | title |
| 重新加载 binge | Reload binge | 错误恢复按钮 |
| 请先在设置中添加 forage 服务器 URL。 | Please add forage server URL in settings first. | 错误消息 |
| forage URL 为 http:// 但 binge 通过 https 加载，浏览器已阻止。请使用 https:// (或 tailnet) 的 forage URL。 | forage URL is http:// but binge loaded via https... | 错误消息 |
| 不会将 Stash API 密钥发送到不受信任的 URL，请使用 https:// 或本地/tailnet 地址。 | Won't send Stash API key to untrusted URL... | 错误消息 |
| 天内你关注的演员没有新场景。 | days, performers you follow have no new scenes. | 空状态 |
| 故事将被静默跳过，直到它重新运行。 | Stories will be silently skipped... | 文本 |
| 此场景暂无评价，选择撰写方式。 | No review yet for this scene... | 文本 |
| 不在你的库中 | not in your library | title |
| 来自 StashDB — 不在你的库中 | From StashDB — not in your library | title |

### 5.12 其他 UI 文本

| 中文 | 英文原文 | 用途 |
|------|---------|------|
| 在 StashDB 打开 | Open on StashDB | 链接文本 |
| 在 Stash 中打开 | Open in Stash | 链接文本 |
| 在 X 上打开 → | Open on X → | 链接文本 |
| 在 PornHub 上打开 → | Open on PornHub → | 链接文本 |
| 在 Reddit 查看 → | View on Reddit → | 链接文本 |
| 观看完整场景 → | Watch full scene → | 链接文本 |
| StashDB 上有 | StashDB has | 文本 |
| O 计数 | O count | aria-label |
| 点击喜欢 · 长按取消 | Click to like · Long-press to unlike | title |
| O 计数 ${e}。点击喜欢，长按取消。 | O count ${e}. Click to like, long-press to unlike. | aria-label |
| 性能调试覆盖层 | Performance debug overlay | aria-label |
| 暂停 | Pause / Break | 键盘键名 (keyCode 19) |
| 首页 | Home | 键盘键名 (keyCode 36) |
| ${e.length} 个其他链接 | ${e.length} other links | title |
| 显示其他链接 | Show other links | aria-label |
| 在 StashDB 打开"${e.title}" | Open "${e.title}" on StashDB | aria-label |
| 在 StashDB 上打开场景 | Open scene on StashDB | aria-label |
| Discard this edit and start a fresh interview? | (未翻译，confirm 对话框) | confirm 消息 |
| Discard this interview/draft? Saved reviews on the scene are not touched. | (未翻译，confirm 对话框) | confirm 消息 |
| 保存中… | Saving… | 按钮状态 |
| 已保存。 | Saved. | 状态消息 |
| 语气风格 — 下一条消息生效 | Tone style — Applies to next message | title |
| 访谈的语气风格（手动撰写时忽略） | Interview tone style (ignored for manual) | title |
| 相同保存目标，只是没有...辅助。 | same save target, just without...assistance. | 设置说明 |
| 模式通过...进行访谈。如果...离线，请手动撰写 | mode via...conducts interview. If...offline, write manually | 设置说明 |

---

## 六、LLM 提示词中的中文（不翻译但已存在）

> 以下中文出现在 LLM prompt 中，属于之前版本的设定，不作为 UI 翻译。

| 中文 | 位置 | 用途 |
|------|------|------|
| 演员： | ac 函数 | LLM prompt，多个演员时 |
| 演员 | G 函数 | LLM prompt，performer 上下文标题 |

---

## 七、还原指南

### 7.1 升级后还原步骤

1. **下载新版本** binge 插件，替换 `index.html`
2. **应用代码修改**（第四部分）:
   - 在 GraphQL 查询区域新增 `bs=` PerformerGalleries 查询
   - 在数据函数区域新增 `pk()` 函数
   - 重构 yf 组件为图库网格 + 灯箱
   - 翻译时间函数（um 和 Bf）
   - 修改 Tab ID `photos` → `galleries`，标签 `照片` → `图库`
   - 新增 mYf 组件实现图库封面悬停自动播放（setTimeout 0.5s + setInterval 1s）
   - 修改 Kf 组件首页图库卡片自动播放延迟为 2s
   - 修改 Gc 组件添加视频 poster 懒加载（useEffect 添加 `!h` 守卫）
3. **应用翻译**（第五部分）:
   - 按类别搜索英文原文，替换为中文
   - 注意压缩 JS 中字符串可能使用反引号或单引号
4. **硬性刷新** (Ctrl+Shift+R) 测试

### 7.2 注意事项

- 压缩后的 JS 变量名每次编译可能不同，搜索时用字符串内容而非变量名
- `;;` → `;` 等全局替换会破坏 `for(;;)` 循环，避免使用
- 函数名避免与 React 内部变量冲突（如 `U`、`W`、`Uf`、`Lu`、`za`）
- 模板字面量中的中文不需要转义
- Stash Gallery 查询不要包含 `image_path` 字段（会 400 错误）
- 可选链 `w?.length>0` 用于处理 null 初始值
- 反引号相关的显示问题是复制工具造成的，不需要修改

---

## 八、性能调优方案

> 以下方案基于当前 index.html 单文件架构提出，可按优先级逐步实施。

### 8.1 构建产物优化（效果最好）

1. **启用 gzip/brotli 压缩**
   - Stash 的静态文件服务通常不压缩 index.html。可在反向代理（Nginx/Caddy）层对 `/plugin/binge/assets/index.html` 开启 `gzip_static` / `brotli_static`
   - 预期收益：单文件 700KB+ 可压缩至 200KB 以内，首字节时间大幅下降

2. **拆分 index.html**
   - 当前为 React 运行时 + 应用逻辑 + CSS 全部打包在一个 HTML 文件
   - 拆分为 `vendor.js`（React/ReactDOM）+ `app.js` + `styles.css` + `index.html`
   - 配合 HTTP 缓存：`vendor.js` 可长期缓存，`app.js` 按版本 hash
   - 预期收益：插件升级后用户只需下载变更的 app.js，不用重复下载整个运行时

3. **代码分割 / 懒加载**
   - 演员档案、设置面板、Scribe 评价等低频功能拆成独立 chunk
   - 使用 `React.lazy()` + `Suspense`，进入对应页面时再加载
   - 预期收益：首页首屏 JS 体积下降 30%–50%

### 8.2 数据加载优化

1. **GraphQL 查询合并**
   - 当前首页初始化可能触发多个独立 GraphQL 请求（配置、插件、场景、演员等）
   - 合并为一次 `query { configuration plugins findScenes ... }`，减少往返延迟
   - 预期收益：首屏请求数从 N 次降到 1 次

2. **图片/缩略图懒加载**
   - Feed 卡片视频 poster 和演员头像进入视口才加载
   - 图库图片使用虚拟滚动 + 分页，避免一次性渲染 500 张图片 DOM
   - 预期收益：内存占用下降，滚动更流畅

3. **缓存故事/推荐数据**
   - `useSharedStories` 当前每次进入页面重新拉取
   - 添加 SWR 策略：本地缓存 5 分钟，后台静默刷新
   - 预期收益：重复访问时首页秒开

### 8.3 渲染优化

1. **减少 Context 重渲染**
   - `useSharedStories` 等 Context 在状态变化时会让所有订阅组件重渲染
   - 使用 `useMemo`/`useCallback` 稳定 Context value，或拆分为多个细粒度 Context
   - 预期收益：滑动 feed 时减少无效渲染

2. **虚拟化长列表**
   - 首页 feed、演员列表、图库图片列表启用虚拟滚动（react-window / react-virtuoso）
   - 预期收益：DOM 节点数固定，长列表不卡顿

3. **CSS 优化**
   - 减少 `backdrop-filter` 使用（GPU 开销大），或在低端设备上通过媒体查询降级
   - 用 CSS 变量统一管理颜色，减少内联 style 导致的样式计算量

### 8.4 网络与媒体优化

1. **视频预加载策略**
   - 当前可能预加载多个视频。改为仅预加载当前及下一个视频，其余用 `preload="metadata"`
   - 预期收益：减少带宽浪费，提升首视频播放速度

2. **HLS/DASH 自适应**
   - 如果 Stash 提供多码率 HLS，优先播放低码率首片段，再根据带宽切换
   - 预期收益：弱网环境下播放更流畅

3. **使用 CDN / 本地缓存**
   - 演员头像、场景 poster 等静态资源设置较长缓存头
   - 如果部署 binge-server，可将其放至同一局域网或启用本地缓存

### 8.5 低优先级/谨慎尝试

1. **Service Worker 离线缓存**
   - 缓存 index.html 和静态资源，离线可访问已加载内容
   - 注意：插件更新后需正确清理旧缓存

2. **WASM 图片解码**
   - 如果图库图片量大，可用 Web Worker + WASM 解码缩略图
   - 复杂度较高，收益有限，建议排在最后

### 8.6 推荐实施顺序

| 优先级 | 方案 | 难度 | 收益 |
|-------|------|------|------|
| P0 | Nginx/Caddy 开启 gzip/brotli | 低 | 高 |
| P0 | 合并首页 GraphQL 查询 | 中 | 高 |
| P1 | 图片懒加载 + 虚拟滚动 | 中 | 高 |
| P1 | 视频预加载策略优化 | 低 | 中 |
| P2 | index.html 拆分为 JS/CSS | 高 | 高 |
| P2 | Context 重渲染优化 | 中 | 中 |
| P3 | 代码分割懒加载 | 高 | 中 |
| P3 | Service Worker 缓存 | 高 | 低 |
